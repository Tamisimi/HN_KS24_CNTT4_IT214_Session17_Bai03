server:
  port: 8085
spring:
  application:
    name: menu-service
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 10
  cache:
    type: redis
